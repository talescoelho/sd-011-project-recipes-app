import React from 'react';
import MealsContext from './MealsContext';

export default function MealsProvider() {
  return (
    <MealsContext.Provider>
      { Children }
    </MealsContext.Provider>
  );
}
