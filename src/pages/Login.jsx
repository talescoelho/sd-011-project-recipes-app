import React, { useState } from 'react';
import { Layout } from '../components';

const Login = () => (
  <Layout>
    <main>
      <form>
        <input
          type="email"
        />
      </form>
    </main>
  </Layout>
);

export default Login;
